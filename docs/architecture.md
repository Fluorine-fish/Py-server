# 系统架构文档

## 整体架构

该系统采用前后端分离的Web架构，主要包含以下几个核心模块：

### 1. 核心模块

#### 串口通信模块
- 基础串口通信处理 (SerialHandler)
- 通信协议解析和封装
- 提醒系统通信接口
- 设备状态监控
- 自动重连机制

#### 视频分析模块
- 实时视频流处理
- 姿势分析
- 情绪识别
- 用眼距离检测

#### 提醒系统
- 久坐检测
- 坐姿监控
- 提醒消息分发
- 语音交互接口

### 2. 辅助模块
- 数据库管理
- 日志记录
- 错误处理
- 性能监控

### 3. 用户界面
- Web界面
- 实时数据展示
- 配置管理
- 历史记录查看

## 模块详细说明

### Web服务器 (app.py)
- 处理HTTP请求
- 路由管理
- 会话管理
- 错误处理

### 视频流处理 (modules/video_stream_module.py)
- 摄像头数据采集
- 图像预处理
- 实时视频流推送
- 帧率控制

### 串口通信 (serial_handler.py)
- 串口连接管理
- 数据帧处理
- 错误检测和恢复
- 设备状态监控

### 数据库管理 (db_handler.py)
- 数据模型定义
- 数据存取操作
- 查询优化
- 数据备份

### 分析模块
- 姿态检测算法
- 情绪识别算法
- 专注度分析
- 视力保护算法

### 串口通信协议

#### 1. 基础帧格式

所有通信帧均采用32字节定长格式，包含：
- 帧头 (1字节): 's'
- 消息类型 (1字节)
- 数据负载 (29字节)
- 帧尾 (1字节): 'e'

#### 2. 消息类型定义
- 0xA0: 控制指令 (上位机->下位机)
- 0xB0: 状态反馈 (下位机->上位机)
- 0xA1: 提醒指令 (上位机->下位机)
- 0xA4: 照明控制指令 (上位机->下位机)

#### 3. 提醒类型字段 (帧字节11)
- 0x00: 无提醒
- 0x01: 久坐提醒
- 0x02: 坐姿提醒
- 0x03: 语音交互

#### 4. 照明控制帧格式

使用帧中预留的字节区域实现照明控制功能：
- 字节12-15: 亮度值 (浮点数, 0.0-100.0)
- 字节16-19: 色温值 (浮点数, 2700.0-6500.0)
- 字节20: 照明模式
  - 0x00: 手动模式
  - 0x01: 自动模式
  - 0x02: 阅读模式
  - 0x03: 休息模式

### 提醒系统架构

#### 1. 检测层
- 姿势检测模块
- 久坐时间监控
- 用眼距离检测
- 情绪分析

#### 2. 决策层
- 提醒规则引擎
- 阈值管理
- 状态记录

#### 3. 执行层
- 串口通信接口
- 机械臂控制
- 语音交互接口

### 前端界面

#### 照明控制页面
- 实现亮度和色温的可视化调节
- 提供四种照明模式的切换
- 实时预览灯光效果
- 支持参数实时保存
- 响应式设计适配不同屏幕尺寸

### 后端服务

#### 照明控制模块
- SerialCommunicationHandler类扩展了照明控制功能
- 支持亮度、色温和模式的精确控制
- 实现参数验证和范围限制
- 提供状态反馈和错误处理机制

## 数据流

1. 数据采集流程
   ```
   摄像头 -> 视频流模块 -> 图像处理 -> 分析模块 -> 数据库
   ```

2. 提醒流程
   ```
   检测模块 -> 决策引擎 -> 串口通信 -> 机械臂执行
   ```

3. 控制流程
   ```
   Web界面 -> 后端服务 -> 串口模块 -> 机械臂
   ```

4. 数据展示流程
   ```
   数据库 -> Web API -> 前端展示 -> 用户界面
   ```

#### 照明控制流程
1. 用户通过Web界面调整参数
2. 前端发送AJAX请求到/api/light_control
3. 后端验证参数并构建控制帧
4. 通过串口发送到下位机
5. 接收下位机状态反馈并更新界面

## 安全架构

1. 数据安全
   - 通信加密
   - 访问控制
   - 数据备份

2. 设备安全
   - 串口权限管理
   - 设备认证
   - 异常恢复

3. 错误处理
   - 异常捕获
   - 错误日志
   - 自动重试

#### 照明控制安全措施
- 参数范围限制
- 输入验证
- 异常处理
- 状态监控

## 扩展性设计

1. 模块化架构
   - 独立的功能模块
   - 标准化接口
   - 可插拔组件

2. API设计
   - RESTful API
   - 版本控制
   - 文档化

3. 提醒系统扩展
   - 自定义提醒规则
   - 新增提醒类型
   - 参数配置化

## 性能优化

1. 通信优化
   - 串口缓冲管理
   - 数据压缩
   - 异步处理

2. 图像处理
   - 分辨率自适应
   - 帧率控制
   - 硬件加速

3. 系统性能
   - 数据库索引
   - 缓存策略
   - 资源回收